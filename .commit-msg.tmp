refactor(savepoints): finish the rename — on-disk artifact + config key + all comments

Mason: single user, pre-launch, will delete + regenerate all generated files,
so there is NO migration to protect. Rename the last holdouts too:
- shadow-repo dir `.aygent/checkpoints.git` -> `.aygent/savepoints.git`
- git config key `aygent.retentiondays` -> `aygent.savepointretentiondays`
- swept every remaining comment across lib.rs/db.rs/savepoint.rs/Settings.tsx

VERIFIED: grep for "checkpoint" (case-insensitive) across all of src-tauri/src
and ui/src now returns ZERO matches. The rename is 100% complete.

NOTE: existing `.aygent/checkpoints.git` folders + old retention config on
Mason's machine are now orphaned by design — he deletes + regenerates. Fresh
installs create `savepoints.git` from the start.
