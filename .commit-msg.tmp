feat(settings+savepoints): reorder Settings, kill dead radios, rename Checkpoints -> Save Points

Mason's list:
- Settings card order is now: Appearance, Providers, Local Models, Memory,
  Save Points, Multi-agent.
- Local Models: removed the dead radio buttons (models are picked per-agent in
  the Agents tab, so a non-selectable info row now — name/size/tool-capability
  + Delete). Copy updated to say models show up per-agent.
- RENAME Checkpoints -> "Save Points" everywhere user-facing (gamer-nerd term):
  sidebar tab ("savepoints" screen id + "Save Points" label), the screen
  (title, empty state, "Save Point now" button, row fallback label), Settings
  card ("Save Points", copy), Agents share-folder copy, and the two Info-stream
  strings in the turn loop. INTERNAL Rust cmd names (checkpoint_*) + the
  checkpoint.rs module keep their names on purpose (renaming identifiers = pure
  churn + breakage risk, zero user benefit); only user-facing text changed.

First compile after the reorder + rename \u2014 flag if cargo/tsc throws.
